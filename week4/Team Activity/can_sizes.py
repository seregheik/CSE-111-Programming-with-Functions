# import math

# def main():
# def compute_volume(radius, height):
#     # this function computes the volume by taking in the parameters radius and height
#     volume = math.pi * radius**2 * height
#     return volume
# def compute_surface_area(radius, height):
#     # this function computes the surface area by taking in the parameters rdius and height 
#     surface_area = 2 * math.pi * radius * (radius + height)
#     return surface_area

# main()